"use server";

import { db } from "@libs/db";

// // should get all membership related to the user
// // should return a list of membership in an Object array
// export const getAllMembershipsByUserId = (userId: string): boolean => {
    
//     return false;
// }
